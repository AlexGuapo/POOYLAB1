// Realiza operaciones matematicas basicas

public class _p05_OperacionesMatematicas {
     public static void main(String[] args) {
        double x, y, suma, resta, mult, div, rest, pot ;

        x=10.5; y=2.5;
        suma=x+y;
        resta=x-y;
        mult=x*y;
        div=x/y;
        rest=x%y;
        pot=Math.pow(x,y);
    }
}